extends Node

var queue = {}
var objects_in_queue = {}
var available_turns = {}

func _ready() -> void:
	pass
	
func add_to_queue(сurrent_status, final_state, number_of_moves) -> void:
	#if(objects_in_queue.has_method(""))
	pass
